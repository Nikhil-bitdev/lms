# Backend Server
PORT=5000
# Optional range to auto-fallback if PORT is busy (inclusive)
PORT_RANGE=5000-5010

# Auth / Security
JWT_SECRET=replace-with-a-strong-random-secret

# Database (SQLite dev default)
SQLITE_STORAGE=server/dev.sqlite
# For Postgres (uncomment and set when migrating)
# DB_DIALECT=postgres
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=lms
# DB_USER=lms_user
# DB_PASSWORD=supersecret

# Client URL (CORS/socket)
CLIENT_URL=http://localhost:5173

# Frontend Overrides (rarely needed if dynamic detection works)
# VITE_API_URL=http://localhost:5000/api
# VITE_API_PORTS=5000,5001,5002
# VITE_API_PORT_RANGE=5000-5005
